@using TacticalHeroes.Admin.Modules.Identity.Entities.Claims.Model

<MudStack Spacing="2">
    <MudStack Row="true"
              AlignItems="AlignItems.Center"
              Justify="Justify.SpaceBetween">
        <div>
            <MudText Typo="Typo.subtitle1">Атрибуты доступа</MudText>
            <MudText Typo="Typo.caption"
                     Color="Color.Secondary">
                Пары типа и значения, назначенные сущности
            </MudText>
        </div>
        <MudButton Variant="Variant.Text"
                   Color="Color.Primary"
                   StartIcon="@Icons.Material.Filled.Add"
                   OnClick="AddClaim">
            Добавить
        </MudButton>
    </MudStack>

    @if (Claims.Count == 0)
    {
        <div class="empty-claims">
            <MudIcon Icon="@Icons.Material.Outlined.KeyOff"
                     Size="Size.Large"
                     Color="Color.Secondary" />
            <MudText Typo="Typo.body2"
                     Color="Color.Secondary">
                Атрибуты пока не добавлены
            </MudText>
        </div>
    }
    else
    {
        @foreach (var claim in Claims)
        {
            <MudGrid @key="claim"
                     Spacing="2"
                     Class="claim-row">
                <MudItem xs="12" md="5">
                    <MudTextField T="string"
                                  @bind-Value="claim.Type"
                                  Label="Тип"
                                  Variant="Variant.Outlined"
                                  Margin="Margin.Dense"
                                  Required="true"
                                  RequiredError="Укажите тип атрибута" />
                </MudItem>
                <MudItem xs="12" md="6">
                    <MudTextField T="string"
                                  @bind-Value="claim.Value"
                                  Label="Значение"
                                  Variant="Variant.Outlined"
                                  Margin="Margin.Dense"
                                  Required="true"
                                  RequiredError="Укажите значение атрибута" />
                </MudItem>
                <MudItem xs="12" md="1"
                         Class="claim-remove">
                    <MudIconButton Icon="@Icons.Material.Outlined.Delete"
                                   Color="Color.Error"
                                   OnClick="@(() => RemoveClaim(claim))"
                                   aria-label="Удалить атрибут" />
                </MudItem>
            </MudGrid>
        }
    }
</MudStack>
