@page "/reset-password"
@layout AccountLayout
@attribute [AllowAnonymous]
@using TacticalHeroes.Admin.Modules.Identity.Features.Authentication.ResetPassword
@using TacticalHeroes.Admin.Modules.Identity.Widgets.Authentication

<PageTitle>Новый пароль · Tactical Heroes</PageTitle>

<AuthenticationShell>
    <ResetPasswordForm UserId="@UserId" PasswordResetToken="@PasswordResetToken" />
</AuthenticationShell>

@code {
    [SupplyParameterFromQuery(Name = "userId")]
    public Guid? UserId { get; set; }

    [SupplyParameterFromQuery(Name = "passwordResetToken")]
    public string? PasswordResetToken { get; set; }
}
