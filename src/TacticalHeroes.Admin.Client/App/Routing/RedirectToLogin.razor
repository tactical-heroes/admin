@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        var returnUrl = Uri.EscapeDataString(Navigation.Uri);
        Navigation.NavigateTo(
            $"/authentication/challenge?returnUrl={returnUrl}",
            forceLoad: true);
    }
}
